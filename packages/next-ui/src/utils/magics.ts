export module UI {
  /** Property used in custom components, where the developer can apply custom types
   *  based on the internally used properties as an optional overriding feature. */
  export const customStylePropKey = 'take';

  /** Property used to apply individual style to a component (sd = style data) */
  export const styleablePropKey = 'sd';

  /** General roundness of all cards and objects in px before theme transformation. */
  export const generalRoundness = 1;

  export const baseTransitionMs = 150;
}
